{
  "framework": "vite",
  "installCommand": "npm --prefix hrms-frontend ci",
  "buildCommand": "npm --prefix hrms-frontend run build",
  "outputDirectory": "hrms-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
